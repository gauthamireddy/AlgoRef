allows user to modify the object without altering the structure of the object. -- structural

steps
1. create an interface(Shape)
2. Create concrete classes implementing the same interface(Shape)
3. Create abstract decorator class implementing the Shape interface.
4. Create concrete decorator class(red Shape decorator) extending the ShapeDecorator class.
5. Use the RedShapeDecorator to decorate Shape objects.